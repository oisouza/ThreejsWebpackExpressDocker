class spriteScript {
    threeMesh
    load
    animate

    constructor(
        threeMesh,
        load,
        animate) {
        this.threeMesh = threeMesh
        this.load = load
        this.animate = animate
    }
}

export { spriteScript }
